# Copie ce fichier en .env (gitignoré) et mets ta clé Gemini (Google AI Studio).
GEMINI_API_KEY="..."

# Optionnel : clé TheSportsDB pour fetch_results (résultats des matchs).
# Laisse vide pour utiliser la clé de test publique gratuite ("123").
# SPORTSDB_KEY="123"
